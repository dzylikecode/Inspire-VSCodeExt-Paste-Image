{
  "name": "md-paste-jpg",
  "displayName": "md-paste-jpg",
  "description": "paste image to jpg format directly when working on the WSL",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.71.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:md-paste-jpg.pasteJPG"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "md-paste-jpg.pasteJPG",
        "title": "paste image from clipboard"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.71.0",
    "@types/glob": "^7.2.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "14.x",
    "eslint": "^8.11.0",
    "glob": "^7.2.0",
    "mocha": "^9.2.2",
    "typescript": "^4.5.5",
    "@vscode/test-electron": "^2.1.3"
  }
}
